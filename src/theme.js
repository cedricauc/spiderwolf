import { createTheme } from "@mui/material";


export const theme = createTheme({
  palette: {
    // header color
    primary: {
      main: '#204B8E',
    },
    //footer color
    secondary: {
      main: '#1B3F6A',
    },
    // background 
    dark: {
      main: '#0C0C0C',
    },
    // text color
    light: {
      main: '#EBEBEB',
      secondary: "white"
    }
  },
})

